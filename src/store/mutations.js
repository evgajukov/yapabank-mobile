export default {
  setSigner(state, signer) {
    state.signer = signer;
  },
  setWallet(state, wallet) {
    state.wallet = wallet;
  },
  setTxList(state, txlist) {
    state.txlist = txlist;
  },
};